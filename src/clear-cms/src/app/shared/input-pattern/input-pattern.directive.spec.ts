import { InputPatternDirective } from './input-pattern.directive';

describe('InputPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPatternDirective();
    expect(directive).toBeTruthy();
  });
});
